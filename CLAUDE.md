# realestate-app

不動産物件情報を管理・検索するWebアプリケーション。

## プロジェクト概要

- **用途**: 不動産物件の検索・閲覧・管理
- **作業ディレクトリ**: `/Users/alfa156gta/Documents/realestate-app`

## Git 運用ルール

### コード変更のたびにGitHubへプッシュすること

コードを変更・追加・削除するたびに、以下の手順を必ず実行すること:

1. 変更内容をステージング:
   ```
   git add <変更ファイル>
   ```
2. コミット (変更内容を簡潔に記述):
   ```
   git commit -m "変更内容の説明"
   ```
3. GitHubへプッシュ:
   ```
   git push origin <ブランチ名>
   ```

### ブランチ戦略

- `main` ブランチ: 本番相当の安定コード
- 機能追加・修正は原則 `feature/<機能名>` ブランチで作業し、完了後に `main` へマージ
- 軽微な修正は `main` に直接コミット可

### コミットメッセージ

- 日本語・英語どちらでも可
- 変更の「何を・なぜ」を簡潔に記述
- 例: `物件一覧ページのページネーションを追加`, `fix: 検索フィルタが正しく動作しない問題を修正`

### 禁止事項

- `main` ブランチへの `force push` は禁止 (緊急時のみ確認の上実施)
- `.env` や認証情報ファイルはコミット・プッシュ禁止
- `--no-verify` でフックをスキップしない

## 開発ガイドライン

- コードを変更する際は、必ずGitHubにプッシュして変更履歴を残す
- 新機能・大きな変更を始める前にブランチを切る
- セキュリティに関わる変更（認証・APIキー・DB接続）は特に慎重に扱う
